# Phase 2 sync (scraper/) and Phase 3 API (api/).
#   python -m venv .venv
#   .venv/Scripts/activate      (Windows)   |   source .venv/bin/activate   (macOS/Linux)
#   pip install -r requirements.txt

# Shared
requests>=2.31
gspread>=6.0
google-auth>=2.28

# Phase 2 sync only
beautifulsoup4>=4.12
lxml>=5.0

# Phase 3 API
Flask>=3.0
python-dotenv>=1.0
gunicorn>=21.2 ; sys_platform != "win32"    # production server; not needed on Windows

# Tests
pytest>=8.0
